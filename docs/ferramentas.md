# Ferramentas

DETRAN SC: Guias de Débitos expõe 1 ferramenta (todas somente leitura).

### 1. `detran_sc_download_guia_consultar`
**Input**: `placa`, `renavam`, `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

DETRAN SC: Guias de Débitos, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN SC: Guias de Débitos
```
